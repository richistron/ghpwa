import React, { useContext } from 'react';
// import AppContext from './AppContext';

const UserInfo: React.FC = () => {
  // const context = useContext(AppContext);
  return (
    <div className={'adfadf'}>
      <div> avatar</div>
      <div> username</div>
      <div> repos</div>
      <div> starts</div>
    </div>
  );
};

export default UserInfo;
